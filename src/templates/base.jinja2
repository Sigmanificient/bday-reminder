<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta
            name="viewport"
            content="width=device-width, user-scalable=yes,
            initial-scale=1.0, maximum-scale=5.0, minimum-scale=1.0"
    >
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    <link rel="stylesheet" href="/static/css/style.css">

    <title>{% block title %}{% endblock %}</title>
</head>
<body>
<header>
    <a class="underline" href="/">Bday Reminder</a>

    <nav class="right-nav">
        {% if session['user'] %}
            <a class="underline" href="{{ url_for('dashboard_page') }}">Dashboard</a>
            <a class="underline" href="{{ url_for('logout') }}">Déconnexion</a>
        {% else %}
            <a class="underline" href="{{ url_for('login_page') }}">Connexion</a>
            <a class="underline" href="{{ url_for('register_page') }}">Inscription</a>
        {% endif %}
    </nav>
</header>

<main>
    {% block content %}{% endblock %}
</main>

{% block scripts %}{% endblock %}
</body>
</html>
